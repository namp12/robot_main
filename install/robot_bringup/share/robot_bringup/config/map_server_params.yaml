map_server:
  ros__parameters:
    use_sim_time: false
    frame_id: map
    # Note: yaml_filename is overridden dynamically in localization/nav2 launch files
    # yaml_filename: "maps/my_room/map.yaml"
